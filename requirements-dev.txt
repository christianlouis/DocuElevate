# Development and testing dependencies
-r requirements.txt

# Testing
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-asyncio>=1.4.0
pytest-mock>=3.12.0
pytest-timeout>=2.4.0  # Per-test timeout enforcement to prevent CI hangs
httpx>=0.28.1  # For async test client
testcontainers>=3.7.1  # For integration tests with real containers
fpdf2>=2.8.0  # For generating test PDF documents in integration tests
minio>=7.2.20  # For MinIO/S3 integration tests
redis>=8.0.0  # For Redis integration tests
boto3>=1.43.40  # For S3 integration tests

# Code quality
ruff>=0.15.20
mypy>=2.2.0
djlint==1.36.4  # 1.40.9 crashes under Python 3.11 multiprocessing in CI

# Documentation (builds the embedded /help site via MkDocs Material)
mkdocs-material>=9.7.6  # MkDocs Material theme – same package used in docs/requirements.txt

# Type stubs for mypy
types-aiofiles>=25.1.0.20260518
types-requests>=2.33.0.20260712
types-paramiko>=5.0.0.20260617

# Security scanning (Ruff includes most security checks from bandit)
pip-audit>=2.10.1  # Dependency vulnerability scanning against OSV/PyPA advisory databases

# Pre-commit hooks
pre-commit>=3.6.0

# License compliance
pip-licenses==5.5.5  # For license compliance checking

# Release automation
python-semantic-release>=10.6.1

types-aiofiles>=24.1.0.20240311  # Type stubs for aiofiles
