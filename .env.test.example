# Test Environment Configuration
# ================================
# This file is a TEMPLATE for test configuration. Copy it to .env.test and fill in your test values.
# 
# cp .env.test.example .env.test
# 
# The .env.test file is gitignored and will NOT be committed to the repository.
# This allows you to safely store API keys and credentials needed for integration tests.
#
# IMPORTANT NOTES:
# - Use DIFFERENT keys/credentials for testing than production
# - Never commit .env.test to the repository
# - These values are loaded automatically when running pytest
# - Tests will use mock values if .env.test is not present (CI/CD friendly)

# **Core Settings**
# Required for basic test functionality
DATABASE_URL=sqlite:///:memory:
REDIS_URL=redis://localhost:6379/1
WORKDIR=/tmp/test_workdir
GOTENBERG_URL=http://localhost:3000

# **Authentication Settings**
# Set to False for most unit tests
AUTH_ENABLED=False
SESSION_SECRET=test_secret_key_for_testing_must_be_at_least_32_characters_long
ADMIN_USERNAME=test_admin
ADMIN_PASSWORD=test_password

# **AI/ML Services - Integration Tests**
# Leave these as test values for unit tests (mocked)
# Replace with REAL TEST API KEYS for integration tests with external services
# NEVER use production keys here!

# OpenAI - For @pytest.mark.requires_external tests
OPENAI_API_KEY=test-key-replace-with-real-test-key-for-integration-tests
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Azure AI - For @pytest.mark.requires_external tests
AZURE_AI_KEY=test-key-replace-with-real-test-key-for-integration-tests
AZURE_REGION=eastus
AZURE_ENDPOINT=https://test.cognitiveservices.azure.com/

# **Storage Providers - Integration Tests**
# For @pytest.mark.requires_external tests
# Use separate test accounts/buckets, never production!

# Amazon S3
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=test-access-key
# AWS_SECRET_ACCESS_KEY=test-secret-key
# S3_BUCKET_NAME=docuelevate-test-bucket
# S3_FOLDER_PREFIX=test-uploads/

# Dropbox
# DROPBOX_APP_KEY=test-app-key
# DROPBOX_APP_SECRET=test-app-secret
# DROPBOX_REFRESH_TOKEN=test-refresh-token
# DROPBOX_FOLDER=/DocuElevate-Tests

# Google Drive
# GOOGLE_DRIVE_CREDENTIALS_JSON={"type":"service_account",...}
# GOOGLE_DRIVE_FOLDER_ID=test-folder-id

# OneDrive
# ONEDRIVE_CLIENT_ID=test-client-id
# ONEDRIVE_CLIENT_SECRET=test-client-secret
# ONEDRIVE_TENANT_ID=common
# ONEDRIVE_REFRESH_TOKEN=test-refresh-token
# ONEDRIVE_FOLDER_PATH=DocuElevate-Tests

# NextCloud
# NEXTCLOUD_UPLOAD_URL=https://nextcloud-test.example.com/remote.php/dav/files/testuser
# NEXTCLOUD_FOLDER=/DocuElevate-Tests
# NEXTCLOUD_USERNAME=test_username
# NEXTCLOUD_PASSWORD=test_password

# Paperless-ngx
# PAPERLESS_HOST=https://paperless-test.example.com
# PAPERLESS_NGX_API_TOKEN=test-api-token

# **Email/IMAP Settings - Integration Tests**
# Use test email accounts only!
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USERNAME=docuelevate.test@gmail.com
# EMAIL_PASSWORD=test_app_password
# EMAIL_USE_TLS=True
# EMAIL_SENDER=DocuElevate Test <docuelevate.test@gmail.com>

# IMAP1_HOST=imap.gmail.com
# IMAP1_PORT=993
# IMAP1_USERNAME=docuelevate.test@gmail.com
# IMAP1_PASSWORD=test_app_password
# IMAP1_SSL=True

# **Notification Settings - Integration Tests**
# Test notification endpoints only
# NOTIFICATION_URLS=discord://test-webhook,mailto://test@example.com
# NOTIFY_ON_TASK_FAILURE=False
# NOTIFY_ON_FILE_PROCESSED=False

# **HTTP Settings**
HTTP_REQUEST_TIMEOUT=30

# **External Hostname**
EXTERNAL_HOSTNAME=localhost
